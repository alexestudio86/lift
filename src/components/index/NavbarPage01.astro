<!-- Navbar -->
<nav>
  <div class="container">
    <ul>
      <li class='hover-white'>
        <a href="#home">
          <span class='d-lg-block'>Inicio</span>
          <i class="fas fa-home d-sm-block"></i>
        </a>
      </li>
      <li class='hover-white'>
        <a href="#whoAre">
          <span class='d-lg-block'>Quienes somos?</span>
          <i class="fas fa-users d-sm-block"></i>
        </a>
      </li>
      <li class='hover-white'>
        <a href="#help">
          <span class='d-lg-block'>Con tu ayuda</span>
          <i class="fas fa-heart d-sm-block"></i>
        </a>
      </li>
      <li class='hover-white'>
        <a href="#here">
          <span class='d-lg-block'>Dona aquí</span>
          <i class="fas fa-hand-holding-usd d-sm-block"></i>
        </a>
      </li>
    </ul>
  </div>
</nav>  


<style>

  nav {
    box-shadow: 0 2px 5px 0 rgba(0,0,0,0.16),0 2px 10px 0 rgba(0,0,0,0.12);
    position: fixed;
    top: 0;
    width: 100%;
    color: inherit;
    opacity: 0.85;
    z-index: 5;
    display: flex;
    align-items: center;
  }

  nav > div > ul {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(20%, 1fr));

  }

  /* Display mobile */
  @media (width <= 576px) {
    nav > div > ul {
      display: grid;
      justify-content:center;
    }
  }

  nav > div > ul > li {
    list-style: none;
    padding: 0;
    cursor: pointer;
    background: var(--color-white);
    color: var(--color-black);
    text-transform: uppercase;
    font-weight: bold;
    border-color: rgba( 99, 175, 212, 0.2 );
    border-width: 0 1px;
    border-style: solid;
  }

  nav > div > ul > li > a{
    text-align: center;
    color: inherit;
    text-decoration: none;
  }

  nav > div > ul > li > a > span, nav > div > ul > li > a > i {
    padding: 8px 16px;
  }

</style>